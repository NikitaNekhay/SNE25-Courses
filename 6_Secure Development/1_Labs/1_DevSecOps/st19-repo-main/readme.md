Run it as: python -m http.server 8000


Display real currency exchange rates (USD and EUR to RUB) using the Central Bank of Russia API (`https://www.cbr-xml-daily.ru/daily_json.js`, which is valid and available in Russia).

Show real weather based on the user's IP (auto-detected via `wttr.in` API) or fallback to basic weather in Moscow if there's an error.

Display the latest 5 RSS news items from RIA.ru (using `https://ria.ru/export/rss2/index.xml`).

Include two sections: one "About this page" with descriptive text, and another "Dots Animation" where clicking triggers a cool particle dots animation on a canvas.